<div class="blog-post">
  <h2 class="blog-post-title"><%= link_to blog.title, blog %></h2>
  <p class="blog-post-meta">Published <%= distance_of_time_in_words(blog.created_at, Time.now) %> ago - <%= link_to blog.topic.title, topic_path(blog.topic) if blog.topic.present? %></p>

  <%= markdown blog.body[0,200] %>
  <p>
    <% if logged_in?(:site_admin) %>
      <%= link_to blog.status, toggle_status_blog_path(blog) %>
      <%= link_to 'Show', blog %>
      <%= link_to 'Edit', edit_blog_path(blog) %>
      <%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure?' } %>
    <% end %>
  </p>
</div>